# TrueForge server URL (default when running locally)
TRUEFORGE_URL=http://localhost:8790

# Azure OpenAI — configure via TrueForge Settings → Models → Azure OpenAI
# Never put real keys here. TrueForge holds them in its connector system.

# Daytona — configure via TrueForge Settings → Sandbox providers
# Never put real keys here.
